spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    username: admin
    url: jdbc:postgresql://localhost:5432/medical-clinic-db
    password: admin
  jpa:
    hibernate:
        ddl-auto: create-drop
    properties:
      javax:
        #USE TO GENERATE SQL SCHEMA SCRIPT FROM JPA ENTITIES
        #persistence:
         # schema-generation:
          #  create-source: metadata
           # scripts:
            #  action: create
             # create-target: src/main/resources/db/changelog/sql/schema.sql
    database-platform:
      org.hibernate.dialect.PostgreSQL9Dialect
  liquibase:
    change-log: classpath:db/changelog/changelog.yml


